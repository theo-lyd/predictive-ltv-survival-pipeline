version: 2

sources:
  - name: raw
    description: Raw source tables ingested into the Bronze layer.
    tables:
      - name: churn
        description: Baseline telecom churn records loaded append-only.
      - name: promotions
        description: Promotion and discount records linked to customers.
      - name: billing
        description: Billing sync records from Airbyte (when enabled).
